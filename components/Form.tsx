import type { NextPage } from "next";
import styled from "styled-components";
import { FormProp } from "./StyledComponents";

const Style = styled(FormProp)`

`;

const Form: NextPage = () => {
    return <Style>

    </Style>
}

export default Form;